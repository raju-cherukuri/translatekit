import { RtlSupportDirectiveDirective } from './rtl-support-directive.directive';

describe('RtlSupportDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RtlSupportDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
